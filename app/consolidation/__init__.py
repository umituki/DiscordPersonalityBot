"""Growth and consolidation (spec 9.5, 12, 23).

Deliberately empty of re-exports: repositories import these models, so eager
imports here would create a cycle through ``app.storage.repositories``.
"""
