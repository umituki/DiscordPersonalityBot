"""External input/output adapters.

Architecture rule: an interface converts external input and output. It owns no
psychology and no persistence rules, and it never writes the database directly
(spec 37).
"""
