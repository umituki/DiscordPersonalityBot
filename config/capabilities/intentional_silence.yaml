# Capability Contract (rebuild spec 4.3): intentional_silence
capability: "intentional_silence"
trigger: "USER message the LLM proposes not answering"
runtime_entry: "ConversationService (response intent stage)"
candidate_source: "SocialInterpretation.response_intent"
hard_gate: "SilencePolicy (direct question / request / correction never silent)"
actual_action: "no outbound, no typing"
success_event: "YUI_INTENTIONAL_SILENCE"
persistence: "events, interaction_decisions"
recovery: "silence needs no recovery; it is a decision, not an in-flight action"
observability: "!yui trace"
acceptance_test: "test_silence_eligibility_matrix"
status: "NOT_STARTED"
note: "rebuild spec 12"
