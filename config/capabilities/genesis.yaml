# Capability Contract (rebuild spec 4.3): genesis
capability: "genesis"
trigger: "rebuild-reset leaves genesis_status=pending; OWNER runs Genesis"
runtime_entry: "GenesisService / genesis pipeline"
candidate_source: "LLM genesis_year / genesis_month / extraction, with critics"
hard_gate: "critics + FIRST BOOT audits"
actual_action: "psychological replay through the normal processor"
success_event: "FIRST_BOOT"
persistence: "genesis_runs, life_years, life_months, life_entities, events, memories"
recovery: "checkpoint/resume; a replayed month is never replayed twice"
observability: "!yui genesis"
acceptance_test: "test_full_genesis_acceptance"
status: "CODE_ONLY"
note: "rebuild spec 34; v2 Genesis exists but is block-based, not annual/monthly"
