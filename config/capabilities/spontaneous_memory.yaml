# Capability Contract (rebuild spec 4.3): spontaneous_memory
capability: "spontaneous_memory"
trigger: "durable completed-activity or NPC-interaction cue offered by SpontaneousMemorySource"
runtime_entry: "AutonomousRuntime.tick -> SpontaneousMemoryCandidates -> DecisionEngine -> SpontaneousMemoryActions.recall"
candidate_source: "persisted spontaneous_memory_cues; candidate construction is read-only and associative retrieval runs only after selection"
hard_gate: "USER-turn deferral, cue cooldown/consumption, semantic relevance, then recall availability; accessibility alone cannot surface a memory"
actual_action: "MemoryEngine.associate followed by one idempotent spontaneously_recalled practice transition"
success_event: "MEMORY_SPONTANEOUSLY_RECALLED"
persistence: "spontaneous_memory_cues, memory_retrievals, episodic_memories recall counters, events, processing_runs, runtime_ticks, decisions"
recovery: "stable cue hash, durable cooldown and consumed_at survive restart; conditional practice transition prevents duplicate strengthening"
observability: "!yui memory spontaneous plus !yui runtime and memory_retrievals by retrieval_group_id"
acceptance_test: "tests/invariants/test_spontaneous_recall_slice.py::test_spontaneous_recall_slice"
status: "E2E_VERIFIED"
note: "rebuild spec 17.6; deterministic offline E2E verifies event, psychology delivery, persistence, restart suppression and exactly-once practice"
