# Capability Contract (rebuild spec 4.3): normal_reply
capability: "normal_reply"
trigger: "USER Discord message"
runtime_entry: "DiscordGateway.handle_message → ConversationService.handle_inbound"
candidate_source: "ConversationEngine (dialogue_act + reply)"
hard_gate: "OutputGuard + ConversationQualityGuard"
actual_action: "DiscordGateway._send"
success_event: "YUI_MESSAGE_SENT"
persistence: "events, conversation_turns, conversation_traces"
recovery: "an unconfirmed send is never recorded as sent (CORE-004)"
observability: "!yui trace / conversation_traces"
acceptance_test: "tests/regression/test_real_conversations.py"
status: "WIRED"
note: "reply exists end to end; SocialInterpretation and grounding are Phase 1-3"
