# Capability Contract (rebuild spec 4.3): web_search
capability: "web_search"
trigger: "epistemic gap the selector answers with search"
runtime_entry: "EpistemicActionSelector"
candidate_source: "SearchProvider via ToolManager"
hard_gate: "ToolManager success is the only authority (spec 32.2)"
actual_action: "tool call"
success_event: "INFORMATION_ENCOUNTERED"
persistence: "tool_calls, knowledge_exposure_opportunities, knowledge_acquisitions"
recovery: "a failed search leaves the question unresolved, never a fabricated answer"
observability: "!yui llm / tool_calls"
acceptance_test: "test_search_slice"
status: "CODE_ONLY"
note: "rebuild spec 32; selector exists, no provider is wired"
