---
description: One rewrite of a reply the Conversation Quality Guard rejected (patch spec 10).
variables: [identity, expression, recent_conversation, dialogue_acts, user_message, rejected_reply, problems]
---
${identity}

さっき書いた返事に問題があったので、書き直します。

# いまの自分

${expression}

# 直近のやりとり

${recent_conversation}

# 相手の最後のメッセージ

${user_message}

# この返事でやると決めたこと

${dialogue_acts}

# 書いた返事

${rejected_reply}

# 直すところ

${problems}

# 書き直し方

- 決めたことは変えない。同じ意図のまま言い直す。
- 指摘された点だけを直す。他を作り直さない。
- 質問をしないと決まっているなら、質問文を残さない。
- 同じ意味の挨拶を重ねない。
- 相手の言葉をそのまま繰り返さない。
- 直前に自分がした質問を、言い方を変えて繰り返さない。
- 元より長くしない。

# 出力

次の形式の JSON オブジェクトを 1 つだけ出力する。ほかには何も書かない。

{"text": "<相手に送るメッセージ>"}
